---
import '@fontsource-variable/inter';
import { SEO, type SEOProps } from 'astro-seo';
import { favicons } from '../constants/favicons';
import '../index.css';

type Props = {
  title?: string;
  description?: string;
  openGraph?: SEOProps['openGraph'];
};

const { title, description, openGraph } = Astro.props as Props;
---

<html lang="en" class="h-full">
	<head>
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width,initial-scale=1" />

		<SEO
			{title}
			{description}
			{openGraph}
			titleDefault="Zak Nesler"
			titleTemplate="%s — Zak Nesler"
			charset="utf-8"
			twitter={{ creator: '@zaknesler' }}
			extend={{
				link: [
					...favicons,
					{ rel: 'manifest', href: '/manifest.json' },
					{ rel: 'sitemap', href: '/sitemap-index.xml' },
				],
			}}
		/>
	</head>

	<body class="selection:bg-blue-500/20 bg-white dark:bg-gray-950 text-gray-700 dark:text-gray-300">
		<div class="w-full h-4 bg-linear-to-r from-blue-700 to-blue-500 dark:from-blue-900 dark:to-blue-800">
		</div>

		<div class="p-8 lg:p-16">
			<slot />
		</div>
	</body>
</html>
